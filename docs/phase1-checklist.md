# Phase 1 Checklist (You)

## Completed in this repo
- [x] Frontend scaffold: React + Vite + TypeScript
- [x] TailwindCSS setup
- [x] Basic routing shell (`/` and `/status`)
- [x] Flask backend stub app
- [x] Endpoint stubs: `/api/score`, `/api/wells`, `/api/submit`
- [x] Cloud Run config placeholder file
- [x] Firestore index placeholder file
- [x] Env var wiring for Maps key and API base URL

## Manual steps still required by you
- [x] Create/confirm Google Cloud project
- [x] Enable Firestore + Cloud Run + Maps APIs
- [x] Create Google Maps API key and put in `frontend/.env`
- [x] Install dependencies and run frontend/backend locally

## External blocker note
- [ ] Project `environment` tag binding is pending organizer/admin IAM (`resourcemanager.tagValueBindings.create` permission required).
